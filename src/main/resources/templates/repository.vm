package ${packageName}.repository;

import ${packageName}.domain.entity.${className}Entity;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.stereotype.Repository;

/**
 * Repository interface for ${className}.
 */
@Repository
public interface ${className}Repository extends JpaRepository<${className}Entity, ${primaryKeyType}>, JpaSpecificationExecutor<${className}Entity> {
    // Add custom query methods here if needed
}
